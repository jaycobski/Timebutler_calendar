/**
 * Simple performance reporter for deployment
 */

import React from 'react';

const PerformanceReporter: React.FC = () => {
  return null; // Invisible component for now
};

export default PerformanceReporter;